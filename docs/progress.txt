[시작] 프로젝트 초기 세팅 완료. 다음: SPEC.md 작성 후 DB 설계 시작.
[2026-06-09] Prisma 스키마 설계 및 마이그레이션 완료.
[2026-06-09] 인증 기능 구현 완료 (AC1~AC10 전부 통과, typecheck 통과).
  구현: utils(prisma/jwt/bcrypt/response/errors), dtos, repositories, services, controllers, routes, middlewares, index.ts
  이슈: tsconfig.test.json에 "types": ["jest","node"] 명시 필요 (자동 탐색 실패)
다음: 인증 기능 Review 단계 (서브에이전트 또는 별도 세션) → 이후 공연 관리 기능 구현
[2026-06-10] 공연 관리 기능 구현 완료 (AC1~AC15 전부 통과, typecheck 통과).
  구현: utils/redis, dtos/concert, repositories(concert/concertZone), services/concert,
        controllers(adminConcert/concert), routes(admin/concert),
        queues/concertStatus.queue + workers/concertStatus.worker, index.ts 라우트 등록
  이슈: BullMQ 내장 ioredis 버전 충돌 → Queue/Worker 모두 connection options({host,port}) 직접 전달로 해결
        Express 5 req.params 타입 → Request<{id:string}> 제네릭으로 해결
다음: 공연 관리 기능 Review → 이후 대기열 시스템 구현
[2026-06-10] 대기열 시스템 구현 완료 (AC1~AC14 전부 통과, typecheck 통과).
  구현: dtos/queue, repositories/queue, services/queue, controllers/queue, routes/queue,
        socket/queue.socket, queues/workers/queue.worker, utils/socket, index.ts Socket.io 연동
  이슈: socket.emit(직접) + broadcastPositions(브로드캐스트) 이중 전송 → once 핸들러 경쟁 조건
        → 직접 emit 제거, broadcastPositions 단일화로 해결
  미결: SPEC.md 입장 처리 단위 → 1명씩(BullMQ 5초 반복 체크)으로 확정
다음: 대기열 시스템 Review → 이후 예매/결제 시스템 구현
[2026-06-11] 예매/결제 시스템 구현 완료 (AC1~AC15 전부 통과, typecheck 통과, 전체 58/58 통과).
  구현: dtos/reservation, repositories/reservation(Lua 원자적 선점),
        services/reservation, controllers(reservation/payment),
        routes(reservation/payment), queues/workers/reservation.worker(BullMQ delayed job),
        index.ts 라우트/워커 등록
  스키마: Payment.paymentKey를 nullable(String?)로 변경 + 마이그레이션 완료
  이슈: BullMQ job ID에 ':' 문자 불가 → 'expiry_${id}' 언더스코어 구분자로 해결
        Prisma generate 누락 시 타입 오류 → 스키마 변경 후 반드시 generate 실행
다음: 예매/결제 시스템 Review → 이후 추가 기능 구현
[2026-06-11] 전체 구현 리뷰 완료 (features.json 33/33 확인, typecheck 통과, 전체 58/58 통과).
  수정: 1) handleWebhook 멱등성 추가 (PENDING 아닌 예매 재처리 방지)
        2) Webhook DONE 시 DB ConcertZone.remainQuantity 차감 (Redis-DB 이중 관리 완성)
        3) countAdmitted에서 redis.keys() → SCAN (블로킹 명령 제거)
  추가 수정: adminUpdateZone redis.set → redis.incrby delta 방식 (소진 재고 유실 방지)
             reservation 응답에 toReservationDetailResponse mapper 적용 (아키텍처 준수)
  미결: Toss Webhook 위변조 방지 (테스트 모드 의도적 생략, CLAUDE.md에 운영 전 TODO 추가)
[2026-06-12] 2차 리뷰 수정 완료 (58/58 통과, typecheck 통과).
  수정: 1) preemptStock 실패(-1/-2) 시 deleteAdmittedKey 추가 (입장 슬롯 소진 처리)
        2) releaseStock Lua 원자화 (INCRBY + DEL 분리 → 단일 스크립트)
        3) broadcastPositions N+1 제거 (ZRANK×N → ZRANGE 1회 + Map 분배)
        4) getAllWaiting 추가 (queue.repository.ts)
  확인: 이중 재고 복구(Fix 2)는 1차 리뷰의 handleWebhook 상단 PENDING 체크로 이미 해결됨
[2026-06-18] Stitch 디자인 작업 시작. 프로젝트 ID: 236975419009576561
  웹 데스크탑 대기열 화면 다크/라이트 2종 생성 완료
  라이트 모드 재생성 (다크 모드 기준 레이아웃 통일, R석·2매 표시 제거)
  생성 HTML 참고본: docs/light_mode_queue_screen.html
[2026-06-19] 프론트엔드 초기 세팅 + QueuePage 정적 UI 구현 완료.
  세팅: client/ — Vite + React + TypeScript + Tailwind CSS v3 (dark mode: class)
  구현: client/src/pages/QueuePage.tsx — 더미 데이터 기반 정적 UI
    - Navbar (로고, 네비, 테마 토글), ConcertCard (좌 40%), QueuePanel (우 60%), Footer
    - 다크/라이트 실시간 전환, pulse 애니메이션, 진행률 바, 통계 카드
  빌드/타입체크 통과 (tsc --noEmit, vite build)
다음: QueuePage API 연결 (WebSocket 대기 순번, 공연 정보 fetch)
[2026-06-20] QueuePage 백엔드 연결 완료 (타입체크 통과, 빌드 통과).
  신규: client/src/api/client.ts — fetch 기반 API 클라이언트 (Authorization 헤더 자동 주입)
  신규: client/src/hooks/useQueue.ts — 대기열 상태 관리 훅
    - 공연 정보 조회: GET /api/concerts?status=ON_SALE (URL ?concertId 없을 시 자동 조회)
    - 대기열 진입/이탈: socket.emit('queue:enter' | 'queue:leave') — HTTP 대신 소켓 이벤트 사용
      (broadcastPositions가 concert:{concertId} 소켓 룸에만 전송하므로 룸 join이 필수)
    - 실시간 업데이트: queue:position 이벤트 → position/total 갱신
    - 입장 처리: queue:admitted 이벤트 → admitted 상태 전환
    - 30초 유예 재접속: beforeunload → sessionStorage 저장 → 재접속 시 queue:reconnect 전송
    - 소켓 연결 상태 "실시간 연결됨" 표시에 반영
  수정: QueuePage.tsx — 6가지 phase 처리 (loading/no_auth/concert_error/idle/in_queue/admitted)
  인증: localStorage.getItem('vibe_token') — Access Token 읽기
다음: 로그인 페이지 구현 또는 예매/결제 화면 구현
[2026-06-21] Stitch 로그인/회원가입 디자인 생성 + LoginPage.tsx 구현 완료 (typecheck·빌드 통과).
  Stitch: 다크(813ad89b) + 라이트(398aa066) 2종 데스크탑 화면 프로젝트에 추가
  구현: client/src/pages/LoginPage.tsx — 정적 UI + 탭 전환 인터랙션
    - 로그인 탭: 이메일·비밀번호, 비밀번호 표시 토글
    - 회원가입 탭: 이메일·비밀번호·비밀번호확인·닉네임, 불일치 실시간 피드백
    - 다크/라이트 테마 전환 (QueuePage와 동일 컬러 팔레트)
    - Navbar·Footer·InputField·PrimaryButton·TabSwitcher 내부 컴포넌트화
    - form onSubmit 핸들러는 TODO 상태 (API 연결 다음 단계)
다음: LoginPage API 연결 (POST /api/auth/login, /api/auth/register) + App.tsx 라우팅 추가
[2026-06-21] LoginPage API 연결 + React Router 라우팅 완료 (typecheck·빌드 통과).
  설치: react-router-dom v7.18.0 (이미 설치됨 확인)
  App.tsx: BrowserRouter + AppRoutes 패턴 (TIL 플랫폼과 동일 구조)
    / → QueuePage, /login → LoginPage, /* → / 리다이렉트
  LoginPage.tsx: API 연결 완료
    - POST /api/auth/login → accessToken·refreshToken localStorage 저장 → navigate('/')
    - POST /api/auth/register → 성공 시 자동 로그인(login API 재호출) → navigate('/')
    - parseApiError(): 401→잘못된 자격증명, 409→중복 이메일, 그 외 서버 메시지
    - AlertBanner: 탭 상단 에러 표시, X버튼 닫기
    - PrimaryButton: isLoading 시 스피너 + "처리 중…" + disabled
    - 탭 전환 시 에러 초기화 (switchTab 함수)
    - 회원가입 불일치 상태에서 submit 시 early return + 버튼 disabled
다음: QueuePage에서 미로그인 시 /login 리다이렉트 처리 (ProtectedRoute 또는 useEffect)