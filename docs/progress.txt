[시작] 프로젝트 초기 세팅 완료. 다음: SPEC.md 작성 후 DB 설계 시작.
[2026-06-09] Prisma 스키마 설계 및 마이그레이션 완료.
[2026-06-09] 인증 기능 구현 완료 (AC1~AC10 전부 통과, typecheck 통과).
  구현: utils(prisma/jwt/bcrypt/response/errors), dtos, repositories, services, controllers, routes, middlewares, index.ts
  이슈: tsconfig.test.json에 "types": ["jest","node"] 명시 필요 (자동 탐색 실패)
다음: 인증 기능 Review 단계 (서브에이전트 또는 별도 세션) → 이후 공연 관리 기능 구현
[2026-06-10] 공연 관리 기능 구현 완료 (AC1~AC15 전부 통과, typecheck 통과).
  구현: utils/redis, dtos/concert, repositories(concert/concertZone), services/concert,
        controllers(adminConcert/concert), routes(admin/concert),
        queues/concertStatus.queue + workers/concertStatus.worker, index.ts 라우트 등록
  이슈: BullMQ 내장 ioredis 버전 충돌 → Queue/Worker 모두 connection options({host,port}) 직접 전달로 해결
        Express 5 req.params 타입 → Request<{id:string}> 제네릭으로 해결
다음: 공연 관리 기능 Review → 이후 대기열 시스템 구현
[2026-06-10] 대기열 시스템 구현 완료 (AC1~AC14 전부 통과, typecheck 통과).
  구현: dtos/queue, repositories/queue, services/queue, controllers/queue, routes/queue,
        socket/queue.socket, queues/workers/queue.worker, utils/socket, index.ts Socket.io 연동
  이슈: socket.emit(직접) + broadcastPositions(브로드캐스트) 이중 전송 → once 핸들러 경쟁 조건
        → 직접 emit 제거, broadcastPositions 단일화로 해결
  미결: SPEC.md 입장 처리 단위 → 1명씩(BullMQ 5초 반복 체크)으로 확정
다음: 대기열 시스템 Review → 이후 예매/결제 시스템 구현
[2026-06-11] 예매/결제 시스템 구현 완료 (AC1~AC15 전부 통과, typecheck 통과, 전체 58/58 통과).
  구현: dtos/reservation, repositories/reservation(Lua 원자적 선점),
        services/reservation, controllers(reservation/payment),
        routes(reservation/payment), queues/workers/reservation.worker(BullMQ delayed job),
        index.ts 라우트/워커 등록
  스키마: Payment.paymentKey를 nullable(String?)로 변경 + 마이그레이션 완료
  이슈: BullMQ job ID에 ':' 문자 불가 → 'expiry_${id}' 언더스코어 구분자로 해결
        Prisma generate 누락 시 타입 오류 → 스키마 변경 후 반드시 generate 실행
다음: 예매/결제 시스템 Review → 이후 추가 기능 구현
[2026-06-11] 전체 구현 리뷰 완료 (features.json 33/33 확인, typecheck 통과, 전체 58/58 통과).
  수정: 1) handleWebhook 멱등성 추가 (PENDING 아닌 예매 재처리 방지)
        2) Webhook DONE 시 DB ConcertZone.remainQuantity 차감 (Redis-DB 이중 관리 완성)
        3) countAdmitted에서 redis.keys() → SCAN (블로킹 명령 제거)
  추가 수정: adminUpdateZone redis.set → redis.incrby delta 방식 (소진 재고 유실 방지)
             reservation 응답에 toReservationDetailResponse mapper 적용 (아키텍처 준수)
  미결: Toss Webhook 위변조 방지 (테스트 모드 의도적 생략, CLAUDE.md에 운영 전 TODO 추가)
[2026-06-12] 2차 리뷰 수정 완료 (58/58 통과, typecheck 통과).
  수정: 1) preemptStock 실패(-1/-2) 시 deleteAdmittedKey 추가 (입장 슬롯 소진 처리)
        2) releaseStock Lua 원자화 (INCRBY + DEL 분리 → 단일 스크립트)
        3) broadcastPositions N+1 제거 (ZRANK×N → ZRANGE 1회 + Map 분배)
        4) getAllWaiting 추가 (queue.repository.ts)
  확인: 이중 재고 복구(Fix 2)는 1차 리뷰의 handleWebhook 상단 PENDING 체크로 이미 해결됨
[2026-06-18] Stitch 디자인 작업 시작. 프로젝트 ID: 236975419009576561
  웹 데스크탑 대기열 화면 다크/라이트 2종 생성 완료
  라이트 모드 재생성 (다크 모드 기준 레이아웃 통일, R석·2매 표시 제거)
  생성 HTML 참고본: docs/light_mode_queue_screen.html
[2026-06-19] 프론트엔드 초기 세팅 + QueuePage 정적 UI 구현 완료.
  세팅: client/ — Vite + React + TypeScript + Tailwind CSS v3 (dark mode: class)
  구현: client/src/pages/QueuePage.tsx — 더미 데이터 기반 정적 UI
    - Navbar (로고, 네비, 테마 토글), ConcertCard (좌 40%), QueuePanel (우 60%), Footer
    - 다크/라이트 실시간 전환, pulse 애니메이션, 진행률 바, 통계 카드
  빌드/타입체크 통과 (tsc --noEmit, vite build)
다음: QueuePage API 연결 (WebSocket 대기 순번, 공연 정보 fetch)
[2026-06-20] QueuePage 백엔드 연결 완료 (타입체크 통과, 빌드 통과).
  신규: client/src/api/client.ts — fetch 기반 API 클라이언트 (Authorization 헤더 자동 주입)
  신규: client/src/hooks/useQueue.ts — 대기열 상태 관리 훅
    - 공연 정보 조회: GET /api/concerts?status=ON_SALE (URL ?concertId 없을 시 자동 조회)
    - 대기열 진입/이탈: socket.emit('queue:enter' | 'queue:leave') — HTTP 대신 소켓 이벤트 사용
      (broadcastPositions가 concert:{concertId} 소켓 룸에만 전송하므로 룸 join이 필수)
    - 실시간 업데이트: queue:position 이벤트 → position/total 갱신
    - 입장 처리: queue:admitted 이벤트 → admitted 상태 전환
    - 30초 유예 재접속: beforeunload → sessionStorage 저장 → 재접속 시 queue:reconnect 전송
    - 소켓 연결 상태 "실시간 연결됨" 표시에 반영
  수정: QueuePage.tsx — 6가지 phase 처리 (loading/no_auth/concert_error/idle/in_queue/admitted)
  인증: localStorage.getItem('vibe_token') — Access Token 읽기
다음: 로그인 페이지 구현 또는 예매/결제 화면 구현
[2026-06-21] Stitch 로그인/회원가입 디자인 생성 + LoginPage.tsx 구현 완료 (typecheck·빌드 통과).
  Stitch: 다크(813ad89b) + 라이트(398aa066) 2종 데스크탑 화면 프로젝트에 추가
  구현: client/src/pages/LoginPage.tsx — 정적 UI + 탭 전환 인터랙션
    - 로그인 탭: 이메일·비밀번호, 비밀번호 표시 토글
    - 회원가입 탭: 이메일·비밀번호·비밀번호확인·닉네임, 불일치 실시간 피드백
    - 다크/라이트 테마 전환 (QueuePage와 동일 컬러 팔레트)
    - Navbar·Footer·InputField·PrimaryButton·TabSwitcher 내부 컴포넌트화
    - form onSubmit 핸들러는 TODO 상태 (API 연결 다음 단계)
다음: LoginPage API 연결 (POST /api/auth/login, /api/auth/register) + App.tsx 라우팅 추가
[2026-06-21] LoginPage API 연결 + React Router 라우팅 완료 (typecheck·빌드 통과).
  설치: react-router-dom v7.18.0 (이미 설치됨 확인)
  App.tsx: BrowserRouter + AppRoutes 패턴 (TIL 플랫폼과 동일 구조)
    / → QueuePage, /login → LoginPage, /* → / 리다이렉트
  LoginPage.tsx: API 연결 완료
    - POST /api/auth/login → accessToken·refreshToken localStorage 저장 → navigate('/')
    - POST /api/auth/register → 성공 시 자동 로그인(login API 재호출) → navigate('/')
    - parseApiError(): 401→잘못된 자격증명, 409→중복 이메일, 그 외 서버 메시지
    - AlertBanner: 탭 상단 에러 표시, X버튼 닫기
    - PrimaryButton: isLoading 시 스피너 + "처리 중…" + disabled
    - 탭 전환 시 에러 초기화 (switchTab 함수)
    - 회원가입 불일치 상태에서 submit 시 early return + 버튼 disabled
다음: QueuePage에서 미로그인 시 /login 리다이렉트 처리 (ProtectedRoute 또는 useEffect)
[2026-06-21] 인증 가드 구현 완료 (typecheck·빌드 통과).
  신규: client/src/components/ProtectedRoute.tsx
    - getToken() 없으면 <Navigate to="/login" replace /> — 동기 체크라 로딩 스피너 불필요
  App.tsx: QueuePage를 ProtectedRoute로 감싸기 (앞으로 추가될 보호 라우트도 동일 패턴)
  QueuePage.tsx:
    - NoAuthView 컴포넌트 제거 (ProtectedRoute가 초기 진입 차단)
    - useEffect: phase === 'no_auth' → navigate('/login', replace) — 세션 중 토큰 소멸 대비
다음: 예매/결제 화면 구현 또는 전체 플로우 E2E 테스트
[2026-06-21] 인증 가드 및 Navbar 닉네임 연동 완료 (typecheck 통과).
  신규: client/src/components/ProtectedRoute.tsx — getToken() 없으면 /login 리다이렉트
  App.tsx: QueuePage를 ProtectedRoute로 감싸 라우터 레벨 인증 가드 적용
  QueuePage.tsx: NoAuthView 제거, no_auth phase → navigate('/login') 세션 중 토큰 소멸 대응
               Navbar 닉네임: 하드코딩 제거 → GET /api/auth/me 응답으로 교체 (로딩 중 shimmer)
  LoginPage.tsx: 이미 로그인 상태에서 /login 진입 시 / 리다이렉트 추가
다음: 공연 목록 페이지 디자인 (Stitch 생성 → ConcertsPage.tsx 구현)
[2026-06-22] 공연 목록 페이지 구현 완료 (typecheck·빌드 통과).
  Stitch: 다크/라이트 2종 데스크탑 화면 프로젝트(236975419009576561)에 추가
  신규: client/src/pages/ConcertsPage.tsx — 정적 더미 데이터 기반 초안
    - 상태 탭 필터 (전체/판매예정/판매중/매진/판매종료), 3열 그리드, hover lift-up
    - 상태 배지 (ON_SALE 펄스 닷), ENDED 취소선, 빈 상태 UI
    - ON_SALE만 "티켓 예매" 버튼 활성 → navigate('/queue/:concertId')
  라우팅 변경: App.tsx에서 / → ConcertsPage, /queue/:concertId → QueuePage 추가
  QueuePage: useConcertId를 useParams 기반으로 교체 (query param 방식 제거)
[2026-06-23] ConcertsPage API 연결 완료 (typecheck·빌드 통과).
  useConcerts 훅: GET /api/concerts 1회 호출 → 탭 전환은 클라이언트 필터링
  로딩: SkeletonCard 3개 shimmer 애니메이션
  에러: ErrorState + "다시 시도" 버튼 (retry 재호출)
  Concert 타입에서 minPrice·priceLabel 제거 (API 응답에 없음)
다음: 공연 상세 페이지 또는 예매/결제 화면 구현
[2026-06-23] Stitch 공연 상세 화면 생성 + ConcertDetailPage.tsx 구현 완료 (typecheck·빌드 통과).
  Stitch: 다크(58883152) + 라이트(2f448695) 2종 데스크탑 화면 프로젝트에 추가
  신규: client/src/pages/ConcertDetailPage.tsx
    - useParams concertId → GET /api/concerts/:id 호출
    - 2단 레이아웃: 좌(58%) 시네마틱 포스터, 우(42%) 공연 정보 카드
    - 구역별 티켓 카드: 잔여석 0 → SOLD OUT/회색, ≤10 → 빨강 마감임박 배지
    - CTA: ON_SALE & 잔여석 있음 → "대기열 진입하기" 활성, 전매진 → 비활성, 그 외 상태별 텍스트
    - 스켈레톤 로딩, 에러(목록으로/다시시도) 상태 처리
    - 안내 박스: 대기열 방식 안내 + 주의사항(경고 스타일)
  수정: App.tsx — /concerts/:concertId 라우트 추가 (ProtectedRoute 적용)
  수정: ConcertsPage.tsx — 카드 클릭 시 /queue/:id → /concerts/:id 로 변경
다음: 예매/결제 화면 구현 또는 전체 플로우 E2E 테스트
[2026-06-24] ReservationPage.tsx 구현 완료 (typecheck·빌드 통과).
  신규: client/src/pages/ReservationPage.tsx
    - 5분 타이머 카운트다운 (useEffect setInterval), 타이머 0 → /concerts/:id 리다이렉트
    - TimerBanner: sticky top-16, 남은시간 진행률바, 1분 이하 animate-pulse + 빨간 그라디언트
    - "시간 연장" 버튼: 1분 이하 & 미연장 시 활성, 클릭 시 300초 리셋 (API 연결은 TODO)
    - GET /api/concerts/:id로 구역 데이터 로드 + GET /api/auth/me 닉네임 병렬 조회
    - ZoneCard: 구역 선택(보라 테두리+check), SOLD OUT(회색 dimmed), 마감임박(빨강 배지)
    - 수량 선택: -/+ 버튼 (1~4, 잔여석 초과 불가), 구역 선택 후 표시
    - 주문 요약 (sticky lg:top-32): 공연/구역/수량/단가/총액, 결제 버튼
    - 결제 버튼: POST /api/reservations 호출 후 alert (TossPayments SDK 연동은 TODO)
    - 로딩 스켈레톤, 에러 상태 처리
  수정: QueuePage.tsx — AdmittedBanner에 "구역 선택하러 가기" 버튼 추가 → navigate('/reservation/:id')
  수정: App.tsx — /reservation/:concertId 라우트 추가 (ProtectedRoute 적용)
다음: TossPayments SDK 연동 또는 예매 내역 페이지 구현
[2026-06-25] ReservationPage.tsx TossPayments 결제 연동 + 시간 연장 API 연결 완료 (typecheck·빌드 통과).
  설치: @tosspayments/tosspayments-sdk
  환경변수: client/.env에 VITE_TOSS_CLIENT_KEY 추가 (테스트 키 직접 설정 필요)
  handlePay 로직:
    1. reservationId 없으면 POST /api/reservations → reservationId·remainSeconds 획득
    2. loadTossPayments(VITE_TOSS_CLIENT_KEY) → payment({ customerKey: userId })
    3. payment.requestPayment({ method: 'CARD', amount, orderId: reservationId, ... }) Promise 방식
       - PC: iframe 결제창 (Promise resolve 시 navigate('/reservations'))
       - 모바일 폴백: successUrl/failUrl redirect 지정 (failUrl은 /reservation/:id?code=...)
    4. UserCancelError 개별 처리 → "결제를 취소했습니다." 에러 메시지
  handleExtend 로직:
    - reservationId 있을 때만 POST /api/reservations/:id/extend
    - 성공 시 remainSeconds 백엔드 값으로 업데이트 + extended=true
  URL 파라미터 처리: 모바일 failUrl 리다이렉트 후 ?code=USER_CANCEL&message=... 에러 표시
  userId: GET /api/auth/me에서 id 필드 추가 조회 → TossPayments customerKey로 사용
다음: 예매 내역 페이지(/reservations) 구현 또는 E2E 테스트
[2026-06-26] Stitch 예매 내역 페이지 생성 완료 (프로젝트 236975419009576561).
  다크 모드: screen a1d082ed (VIBE TICKETS - My Reservations Dark)
  라이트 모드: screen dfc1b921 (VIBE TICKETS - My Reservations Light)
  디자인 요소: 예매카드 3종 상태 배지(CONFIRMED/PENDING/EXPIRED), 취소 버튼, 빈 상태 UI
다음: ReservationsPage.tsx 구현 (예매 내역 목록 + 취소 기능)
[2026-06-26] ReservationsPage.tsx 구현 완료 (typecheck·빌드 통과).
  신규: client/src/pages/ReservationsPage.tsx
    - useReservations 훅: GET /api/reservations 호출
    - 스켈레톤 로딩(3개), 에러 상태(재시도), 빈 상태("공연 보러가기" → /)
    - ReservationCard: 공연명/아티스트/날짜/장소/구역+수량/금액/예매일 + 상태 배지
    - 상태 배지: PENDING(노랑), CONFIRMED(초록), CANCELLED(회색), EXPIRED(빨강)
    - PENDING만 "예매 취소" 버튼 → CancelDialog (확인/취소) → DELETE /api/reservations/:id → retry
    - Navbar: 마이페이지 active, 로고/공연 버튼 navigate('/'), 테마 토글
  수정: App.tsx — /reservations 라우트 추가 (ProtectedRoute)
  수정: ConcertsPage.tsx — Navbar 마이페이지 버튼 navigate('/reservations') 연결
  브라우저 테스트: WSL 환경 shared library 누락으로 headless 불가
    → curl 기반 API 검증 통과 (GET/EXPIRED취소400/상세조회), 빌드·타입체크 정상
다음: Navbar 컴포넌트에 닉네임 클릭시 드롭다운 추가 + 다크/라이트 모드 변환 오류 수정
[2026-06-26] 전체 Navbar 드롭다운 메뉴 구현 완료 (typecheck·빌드 통과).
  구현: 5개 페이지(ConcertsPage/ConcertDetailPage/QueuePage/ReservationPage/ReservationsPage) Navbar
    - @radix-ui/react-dropdown-menu 기반 DropdownMenu 적용
    - 닉네임 + ChevronDown 아이콘 → 클릭 시 드롭다운 (애니메이션 포함)
    - "예매 내역" → navigate('/reservations')
    - 구분선 → "로그아웃" → POST /api/auth/logout → localStorage.clear() → navigate('/login')
    - 다크/라이트 테마별 명시적 색상 (Portal 렌더로 CSS 변수 미적용 → inline className으로 처리)
  신규: client/src/components/ui/dropdown-menu.tsx (Radix UI 기반 shadcn 스타일)
  신규: client/src/lib/utils.ts (cn 헬퍼)
  수정: client/src/index.css (Tailwind v3 호환 CSS 변수 재작성)
  ConcertDetailPage: 로고 클릭 navigate('/'), 네비 버튼 navigate 연결 추가
  ReservationPage: 네비 버튼 nav 구조 개선 (마이페이지 → /reservations 연결)
[2026-06-26] Navbar 공통 컴포넌트 추출 완료 (typecheck·빌드 통과).
  신규: client/src/components/Navbar.tsx — useLocation() 기반 active 자동 감지, handleLogout 내장
  5개 페이지 로컬 Navbar 함수 제거, import 단일화
  LoginPage는 미인증용 단순 Navbar라 분리 유지
[2026-06-27] Stitch 어드민 공연 관리 페이지 생성 완료 (프로젝트 236975419009576561).
  다크 모드: screen ca23f5c0 (VIBE TICKETS Admin Concert Management - Dark)
  라이트 모드: screen 368307b3 (VIBE TICKETS Admin Concert Management - Light)
  포함 요소: ADMIN 배지 헤더, 공연 목록 테이블(4행·상태배지·수정/삭제 액션),
             판매현황 패널(구역별 수량·판매율 progress bar), 공연등록 모달, 빈상태 UI
[2026-06-27] AdminPage.tsx 구현 완료 (typecheck·빌드 통과).
  신규: client/src/pages/AdminPage.tsx
    - JWT 역할 기반 진입 (AdminRoute: ADMIN 아닌 경우 / 리다이렉트)
    - 공연 목록 테이블 (GET /api/concerts) — 행 클릭 시 판매 통계 패널 표시
    - 판매 통계 패널 (GET /api/admin/concerts/:id/stats) — 구역별 수량/판매율 진행률 바
    - 공연 등록/수정 모달 (shadcn Dialog + @radix-ui/react-dialog)
    - 공연 삭제 확인 다이얼로그
    - 구역 수정 모달 (PATCH /api/admin/zones/:id)
  신규: client/src/components/AdminRoute.tsx — ADMIN role guard
  신규: client/src/components/ui/dialog.tsx — Radix UI 기반 Dialog
  수정: client/src/api/client.ts — api.patch 추가, getRole() 함수 추가
  수정: client/src/components/Navbar.tsx — ADMIN 역할 시 "관리자 페이지" 드롭다운 항목 표시, adminBadge prop 추가
  수정: client/src/App.tsx — /admin 라우트 (AdminRoute) 추가

Known Issues (추후 수정 필요):
1. AdminPage 다크/라이트 테마 토글 시 헤더만 전환되고
   body(테이블, 판매 현황 패널)는 다크 모드 유지되는 버그
   → AdminPage.tsx의 다크/라이트 조건부 클래스 전체 점검 필요
[2026-06-30] AdminPage 공연 등록/수정 모달에 구역 등록 기능 추가 (typecheck·빌드 통과).
  등록: 구역행 동적 추가/삭제, 공연 생성 후 구역 순차 POST
  수정: 기존 구역 읽기 전용 표시 + 새 구역 추가
다음: README 작성 → 프로젝트 최종 완성
[2026-07-01] 다크/라이트 테마 토글 버그 수정 완료 (typecheck·빌드 통과).
  원인: 각 페이지가 로컬 useState(true)로 dark 관리 → 토글 시 해당 컴포넌트만 갱신, body 미반영
  수정: ThemeContext(localStorage 초기화, document.documentElement.classList 동기화) 신규 생성
        App.tsx에 ThemeProvider 주입, Navbar에서 useTheme()로 전역 토글
        6개 페이지(ConcertsPage/ConcertDetailPage/QueuePage/ReservationPage/ReservationsPage/AdminPage):
          local useState 제거 → useTheme() 교체, 외부 wrapper div → fragment(<>) 교체
        AdminPage body(테이블·StatsPanel·모달·입력필드) 전체 조건부 색상 추가
다음: README 작성 → 프로젝트 최종 완성
[2026-07-02] 고객센터 게시판 백엔드 구현 완료 (AC1~AC17 전부 통과, typecheck 통과, 전체 75/75 통과).
  마이그레이션: add-post-reply (Post, Reply 모델 + PostStatus enum)
  구현: dtos/post, repositories/post, services/post,
        controllers(post/adminPost), routes(post/adminPost), index.ts 라우트 등록
  설계: Reply 생성 + Post.status ANSWERED 원자화 ($transaction), 삭제 검증 순서(404→403→400)
다음: 고객센터 게시판 프론트엔드 구현 (Stitch 디자인 생성 → SupportPage.tsx)
[2026-07-03] Stitch 고객센터 페이지 디자인 생성 완료 (프로젝트 236975419009576561).
  유저 고객센터 다크: screen 80f01cbd (문의 목록 + 문의 작성 모달 오버레이)
  유저 고객센터 라이트: screen 4b9b147b (문의 목록 + 문의 상세/답변 완료 모달 오버레이)
  어드민 문의 관리 다크: screen d125af52 (전체 문의 목록 + 답변 등록 모달 오버레이)
  어드민 문의 관리 라이트: screen 3886cb44 (전체 문의 목록 + 답변 수정 모달 오버레이)
다음: SupportPage.tsx 구현 (유저 고객센터 /support) + AdminSupportPage.tsx 구현 (/admin/support)
[2026-07-04] 고객센터 프론트엔드 구현 완료 (typecheck 통과, 빌드 통과, post 17/17 통과).
  백엔드 수정: findAllPosts에 content·reply 필드 추가 → AdminPostSummaryResponse 확장
  신규: client/src/pages/SupportPage.tsx (/support)
    - 문의 목록 테이블 (번호/제목/상태배지/작성일/삭제버튼), 행 클릭 → 상세 모달
    - 문의 작성 모달 (POST /api/posts), 문의 상세 모달 (GET /api/posts/:id, 답변 표시)
    - PENDING 문의 삭제 확인 다이얼로그 (DELETE /api/posts/:id)
    - 로딩 스켈레톤, 빈 상태, 에러 상태 처리
  신규: client/src/pages/AdminSupportPage.tsx (/admin/support)
    - 전체 문의 목록 테이블 (번호/작성자닉네임/제목/상태/작성일/액션)
    - 답변 모달: PENDING → POST /api/admin/posts/:id/reply, ANSWERED → PATCH (isEdit 분기)
    - 통계 배지: 대기 N건 / 완료 N건
  수정: Navbar.tsx — '티켓팅' 제거, '고객센터' → /support, ADMIN 드롭다운에 "고객센터 관리" 추가
  수정: App.tsx — /support (ProtectedRoute), /admin/support (AdminRoute) 추가
다음: README 작성 → 프로젝트 최종 완성
[2026-07-07] payments 로깅 추가 (Webhook 디버깅 목적).
  신규: src/utils/logger.ts — pino 로거 (dev: pino-pretty 컬러 출력, prod: level info)
  수정: src/index.ts — pino-http 미들웨어 전역 등록 (요청 단위 로깅)
  수정: src/controllers/payment.controller.ts — tossWebhookHandler에 수신 body·Authorization 헤더·처리 완료/실패(스택 포함) 로그 추가
  배경: Toss Webhook 로컬 수신 시 실패 원인 추적 어려움 → 운영 전 Authorization 시크릿 키 헤더 검증 적용 시에도 재사용 가능
[2026-07-09] ngrok 연동 후 발견된 Webhook 400 에러 수정 완료 (전체 77/77 통과, typecheck 통과).
  원인: TossWebhookDto가 paymentKey/orderId/status를 최상위 필드로 기대했으나
        실제 토스 Webhook은 { eventType, data: {...} } 봉투 구조로 옴 → safeParse 실패로 400
        (이 시점이 try/catch 밖이라 [WEBHOOK] 처리 실패 상세 로그도 안 찍혔던 것)
  수정: dtos/reservation.dto.ts — TossWebhookDto를 eventType/data 봉투 구조로 변경
        controllers/payment.controller.ts — handleWebhook에 parsed.data.data 전달,
          형식 검증 실패 시에도 logger.warn(issues 포함)으로 원인 기록하도록 추가
        services/reservation.service.ts — handleWebhook에 status EXPIRED 분기 추가
          (Reservation EXPIRED + Payment FAILED + 재고 복구, ReservationStatus enum 기존 EXPIRED 값 재사용)
  테스트: tests/integration/reservation.test.ts — AC14 웹훅 body를 실제 봉투 구조로 갱신,
          AC15(EXPIRED) 케이스 + 잘못된 형식(400) 케이스 신규 추가
  미결: 우리 쪽 BullMQ 5분 만료 워커와 토스 EXPIRED Webhook 중 어느 쪽이 먼저 도착하든
        멱등성 체크(status !== PENDING → return)로 결과는 동일하나, 실제 ngrok 환경에서
        토스가 보내는 EXPIRED 타이밍 자체는 아직 실측 확인 전
[2026-07-10] README.md 전면 개편 (백엔드+프론트엔드 통합, typecheck/테스트 영향 없음).
  구성: 기술 스택 badge, mermaid 시퀀스 다이어그램(티켓팅 플로우), mermaid ER 다이어그램(데이터 모델),
        프론트엔드 섹션 신규(페이지/라우트 표, 주요 구현 포인트), Webhook payload 예시,
        트러블슈팅 섹션(Socket.io 이중 전송, Toss Webhook 봉투 구조, BullMQ job ID, JWT jti, $transaction 배열 구조분해)
  갱신: 테스트 스위트 5개/77 케이스로 카운트 갱신, 프로젝트 구조에 client/src 추가
[2026-07-12] payment.controller.ts Webhook 검증 방식을 전역 에러 핸들러 패턴으로 통일 (typecheck·테스트 통과).
  변경: safeParse + 수동 AppError(400) → parse() + ZodError catch(로그용) → rethrow
        (다른 컨트롤러 전부 parse()로 던지고 errorHandler가 ZodError를 400으로 변환하는 패턴,
         payment.controller.ts만 예외였던 걸 통일. 에러 응답 메시지도 이제 실제 Zod 검증 메시지로 나감)
[2026-07-13] AdminPage.tsx 저장 실패 시 에러 메시지 표시 버그 수정 (typecheck·빌드 통과, 백엔드 77/77 회귀 없음).
  버그: catch에서 err.message(Axios 자체 메시지, 예: "Request failed with status code 400")를 그대로 노출 →
        실제 백엔드 검증 메시지(err.response.data.message)는 버려지고 있었음
  신규: client/src/lib/errors.ts — getErrorMessage(err, fallback) 공용 헬퍼
        (axios 에러면 response.data.message 우선, 아니면 Error.message, 둘 다 없으면 fallback)
  수정: AdminPage.tsx 공연/구역 저장 실패 catch 2곳을 getErrorMessage로 교체
  참고: ReservationPage.tsx는 이미 인라인으로 동일 로직을 올바르게 쓰고 있어 손대지 않음
[2026-07-14] ReservationPage.tsx 에러 메시지 처리를 getErrorMessage 공용 헬퍼로 통일 (typecheck·빌드 통과).
  대상 3곳: 공연 정보 조회 실패, 시간 연장 실패, 결제 실패(Toss USER_CANCEL은 기존 분기 유지)
  개선: axios.isAxiosError 인라인 중복 제거 + (err as Error).message 안전하지 않은 캐스팅 제거
        (getErrorMessage 내부에서 instanceof Error로 안전하게 체크)
        서버 메시지 없을 때 raw Axios 영문 메시지 대신 한글 fallback 노출 (UX 개선)
  수정: axios import 미사용으로 제거 (noUnusedLocals)
[2026-07-15] dtos/reservation.dto.ts 중복 타입 제거 (typecheck·전체 77/77 통과).
  발견: ReservationDetailInput 타입이 ReservationDetailResponse와 필드가 완전히 동일한 복붙 중복이었고
        다른 곳에서 참조되지 않는(toReservationDetailResponse 파라미터로만 쓰임) 죽은 정의였음
  수정: ReservationDetailInput 삭제, toReservationDetailResponse 파라미터를 ReservationDetailResponse로 직접 사용
        런타임 동작 변화 없음 (타입 레벨 정리)
[2026-07-17] 미사용 repository 함수 2개 삭제 (typecheck·전체 77/77 통과).
  삭제: findZonesByConcertId (concertZone.repository.ts) — 어디서도 호출 안 됨,
        findPaymentByReservationId (reservation.repository.ts) — 어디서도 호출 안 됨
  확인: 전체 src/tests 대상 grep으로 참조 0건 확인 후 삭제
  참고: 재검증 중 reservation.test.ts에서 beforeAll 5초 타임아웃으로 17건 실패가 두 차례 재현됐으나,
        원인은 코드 변경이 아니라 VSCode 세션 기동으로 인한 시스템 리소스 경합(여유 메모리 급감)이었음.
        부하가 가라앉은 후 단일 파일·전체 스위트 모두 재실행해 정상 통과 확인.
[2026-07-18] 미사용 DTO 타입 6개 삭제 (typecheck·전체 77/77 통과).
  삭제: RefreshInput/LogoutInput(auth.dto.ts), ConcertStatusQuery(concert.dto.ts),
        PostSummaryResponse/PostDetailResponse(post.dto.ts) — 삭제로 인해 그 안에서만 쓰이던
        ReplyResponse도 연쇄적으로 죽은 코드가 되어 함께 삭제, ReservationResponse(reservation.dto.ts)
  확인: src/tests 전체 grep으로 참조 0건(선언 라인 외 사용처 없음) 확인 후 삭제, client/ 쪽도 참조 없음 확인
  관찰(조치 안 함): PostSummaryResponse/PostDetailResponse는 plan.md에 명시된 응답 계약이었지만
        post.controller.ts/service.ts가 실제로는 Prisma 엔티티를 그대로 반환하고 있어(매퍼 미적용)
        애초에 연결이 안 된 죽은 타입이었음. AdminPostSummaryResponse는 반대로 adminPost.controller.ts에서
        정상적으로 매퍼로 연결돼 있어 대조됨. 유저 쪽 Post 응답에 매퍼를 실제로 연결하는 건
        범위가 더 큰 작업이라 이번엔 손대지 않음 — 필요하면 별도로 논의.
  환경 메모: 이번에도 전체 스위트 1차 실행에서 reservation.test.ts beforeAll 타임아웃 17건 재현
        (WSL 3.7GB 메모리에 VSCode 세션이 겹쳐 여유 메모리 800MB 안팎일 때 발생 패턴 반복 확인).
        재실행 시 정상 통과 — 코드 문제 아님, 세션 내 반복 관찰된 환경 특이사항으로 기록.
다음: 최종 마무리 점검 (남은 미결 사항 없는지 확인) 또는 사용자 지시 대기